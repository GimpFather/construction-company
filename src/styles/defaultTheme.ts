import { createTheme } from "@mui/material";

export const theme = createTheme({
  palette: {
    primary: {
      main: "#3d5a80",
      light: "#98c1d9",
    },
    secondary: {
      main: "#ee6c4d",
    },
    common: {
      black: "#293241",
      white: "#FAF8F6",
    },
  },
});
